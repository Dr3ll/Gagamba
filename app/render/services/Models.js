define(
    [
        'app',
        'angular'
    ],
    function (app, angular) {
        'use strict';

        app.factory('Character', ['$resource',
            function ($rootScope, ) {
                return {

                };

            }]);
    }
);